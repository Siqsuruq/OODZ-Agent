[plugin]
name = fossil_info
description = Show metadata for the current Fossil checkout and its checked-out version. This read-only tool accepts no model-selected command, version, repository, or path.
permission = read
entrypoint = plugin.tcl
handler = ::plugins::fossil_info::execute
parameters = {"type":"object","properties":{},"required":[],"additionalProperties":false}

[settings]
executable = fossil
