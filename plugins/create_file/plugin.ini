[plugin]
name = create_file
description = Creates a new file inside workspace
category = filesystem
keywords = file,directory,workspace
permission = write
entrypoint = plugin.tcl
handler = ::plugins::create_file::execute
parameters = {"type":"object","properties":{"newfile":{"type":"string","description":"New file relative to the workspace root."}},"required":["newfile"],"additionalProperties":false}