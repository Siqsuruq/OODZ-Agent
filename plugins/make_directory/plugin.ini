[plugin]
name = make_directory
description = Create a directory and any missing parent directories inside the workspace.
permission = write
entrypoint = plugin.tcl
handler = ::plugins::make_directory::execute
parameters = {"type":"object","properties":{"path":{"type":"string","description":"Directory path relative to the workspace root."}},"required":["path"],"additionalProperties":false}
