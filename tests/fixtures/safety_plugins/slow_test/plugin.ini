[plugin]
name = slow_test
description = Test-only slow plugin.
permission = read
entrypoint = plugin.tcl
handler = ::plugins::slow_test::execute
parameters = {"type":"object","properties":{},"required":[],"additionalProperties":false}
